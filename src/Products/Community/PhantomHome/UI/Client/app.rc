// app.rc — ShadowStrike PhantomHome UI executable resources.
//
// The primary icon (ID 1) is what Windows Explorer, the taskbar, and
// Qt's ApplicationWindow default window decoration all pick up.  Keep
// ID 1 on the branded .ico or the shipped UI will show the generic
// blank-page exe icon.
//
// Path is resolved relative to this .rc file's directory by the
// resource compiler; we share the canonical multi-resolution ICO used
// by the installer so the brand stays in sync across artefacts.

#include <winresrc.h>

// Icon ID 1 — Windows picks the lowest-numbered icon resource for Explorer,
// the taskbar, and as the default window icon.
1 ICON "..\\..\\..\\..\\..\\..\\packaging\\installer\\icons\\ShadowStrike.ico"

// This block used to be written out by hand here and said 1.0.0.0 while the
// product shipped as 1.0.91 - the only binary in the tree that carried a version
// was carrying the wrong one. It now comes from src/VersionInfo.h like every
// other binary, so there is one number to change.
#define SS_RC_FILE_DESCRIPTION  "ShadowStrike Phantom Home UI"
#define SS_RC_INTERNAL_NAME     "ShadowStrikePhantomUI"
#define SS_RC_ORIGINAL_FILENAME "ShadowStrikePhantomUI.exe"

#include "..\\..\\..\\..\\..\\VersionInfo.rc.inc"
